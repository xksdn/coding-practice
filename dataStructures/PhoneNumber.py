class number:
    def __init__(self, num, name):
        self.num = num
        self.name = name

    def input(self, name, num):
        list = {}
        name = input("이름을 입력해주세요: ")
        num = input("전화번호를 입력해주세요: ")
        if num in list:
            print("이미 있는 전화번호입니다.")
            


        list.keys(name)
        list.values(num)

